package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Executive Slate & Bronze Gold Palette
val DeepSlateBg = Color(0xFF0B0F19)       // Deep luxury graphite / dark background
val SoftCardBg = Color(0xFF161E2E)        // Soft midnight surface cards
val HotelGold = Color(0xFFDFBA73)         // Sophisticated metallic gold accent
val SoftGold = Color(0xFFF1DCA1)          // Highlighting cream gold
val ActiveBlue = Color(0xFF0F7DFF)        // High-contrast action blue

// Clear Semantic Status Accents
val EmeraldGreen = Color(0xFF2ECC71)      // Confirmed, checked-out (Completed)
val AmberPending = Color(0xFFF39C12)      // Pending confirmation
val CrimsonCancel = Color(0xFFE74C3C)      // Cancelled
val InfoIndigo = Color(0xFF8E44AD)        // Room conference, meeting

// Light scheme fallbacks
val HighContrastLightBg = Color(0xFFF4F6FB)
val SoftLightCard = Color(0xFFFFFFFF)
val PrimaryLightNavy = Color(0xFF1B3152)
